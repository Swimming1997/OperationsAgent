# 运营情报中心 · 数据库设计

> 版本：v1（2026-06-13，本地优先架构）
> 定位：数据库表结构的事实来源，是建表与迁移的依据。配套《产品需求文档 v2》《开发顺序与技术方案》。
> 目标：高质量、可扩展、可移植、高性能、不过度设计。

## 0. 设计原则

1. **对齐三件事**：采集能产出什么、产品要展示什么、数据怎么流转——不设计采集填不满或展示用不上的字段。
2. **本地优先双库**：员工本地存全量情报流水（SQLite），中央只存联机协调数据 + 精华素材库（PostgreSQL）。
3. **能合并就合并，能字段就别拆表**：减少 join。据此把详情并入内容、会话/Profile 留本地、对标更新用字段+查询替代通知表。
4. **去重一份**：内容/博主按平台业务键全局唯一（本地库内）。
5. **指标只存最新**：不存历史快照，记数据更新时间。
6. **媒体只存 URL**：不下载；仅"加素材库"时缓存封面。
7. **挂载式可扩展**：AI / 精细化标签 / 回复后续一律"加表挂在稳定主键"，不改核心表。
8. **全在关系库内**：向量用 pgvector、检索用 pg_trgm（中央）/ 本地用 SQLite FTS5，不引入外部组件。

## 1. 通用约定

- 本地库主键：`id INTEGER PRIMARY KEY`（SQLite 自增）；中央库主键：`id BIGINT GENERATED ALWAYS AS IDENTITY`（紧凑、join 快）。
- 业务唯一键：另设唯一约束（如 `平台+平台内容ID`），用于去重与 upsert。
- 时间统一 `TIMESTAMP`（中央用 `TIMESTAMPTZ`）；半结构化用 `JSON`（中央 `JSONB` + 按需 GIN）；大文本 `TEXT`。
- `↗` 表示外键。

---

## 2. 库与表总览

### 2.1 本地库（每个员工电脑一份，SQLite）—— 情报流水 7 张

| 层 | 表 | 作用 |
|---|---|---|
| 情报 | `creator` | 博主画像（去重一份）|
| 情报 | `content` | 内容（含详情，去重一份）|
| 情报 | `content_source` | 来源（对标/搜索/推荐 + 监控时间）|
| 情报 | `comment_hit` | 获客信号命中评论 |
| 搜索 | `search_suggestion` | 长尾词联想 |
| 调度 | `collect_task` | 本地采集意图（搜索/对标/推荐，含定时、上次查看）|
| 调度 | `collect_run` | 一次执行记录（重试/审计）|

### 2.2 中央库（PostgreSQL）—— 只存"筛选打标签的素材库精华" + 联机协调（**全部复用同事已建表**）

> 原则：中央**不放全平台全量流水**，只存被员工收进素材库的精华。以下表同事重构里**都已建好**，本阶段**复用、按需补字段**，不新建 material/collection。

| 层 | 复用的现有表 | 作用 | 改造点 |
|---|---|---|---|
| 用户 | `employees` / `users` | 系统使用者、数据归属 | 复用 |
| 采集端 | `local_agents` | 员工电脑采集程序实例 | 复用 |
| 采集端 | `platform_accounts` | 账号池（Profile/会话留本地）| 复用 + 补限速字段 |
| 对标配置 | `creator_monitors` / `benchmark_groups` | 对标博主登记 + 定时配置同步 | 复用（换机/交接可拿回）|
| 获客规则 | `keyword_rule*` / `operation_rules` | 获客/关键词规则 | 复用 |
| **素材库精华** | **`content_identity` + `content_snapshots`** | 只承载被收进素材库的精华内容快照（**非全量**）| 复用，补话题/平台标签/视频时长/广告/IP 属地 |
| **素材库收藏** | **`reference_library_items`** | 收藏：owner(`created_by_employee_id`) + 时间(`selected_at`) + 标签(`material_tags_json`) + 评分/note | ★ 直接复用，正是要的素材库 |
| 标签 | `manual_tags` / `content_manual_tags` | 标签体系 | 复用 |

**关键纠偏**：之前臆造的 `material`/`collection` 作废——`reference_library_items` 已完全覆盖"谁/何时/什么标签"，主管全局产出按 `created_by_employee_id` 聚合即可。

**扩展预留（不建）**：AI `content_ai_analysis`/`content_embedding`（锚点 content_identity.id）、回复 `dm_*`/`comment_reply`（锚点 platform_accounts.id）。

---

## 3. 数据流转

```
本地：Agent 采集 → 本地 upsert
  → creator（画像去重，更新最新）
  → content（按平台+内容ID去重，更新最新指标；详情按需填）
  → content_source（本次来源+监控时间）
  → (获客) comment_hit + 回写 content.acquisition_*
  → (长尾) search_suggestion
本地展示：员工浏览器 → 本地 Web 读 content/creator/content_source → 列表/详情
对标提醒：collect_task.last_viewed_at vs content_source.first_seen_at → 新内容数

中央（只收精华，不收全量）：员工"加素材库" → 上报这一条
  → upsert content_identity + content_snapshots（仅该精华内容，按平台+内容ID去重，缓存封面）
  → insert reference_library_items（created_by_employee_id + selected_at + material_tags_json）
联机：local_agents 注册/心跳；platform_accounts 账号池；creator_monitors 对标配置同步
```

---

## 4. 本地库表定义（SQLite）

**4.1 `creator`**（博主画像，去重一份）
id / platform / platform_user_id / nickname / avatar_url / fans_count / total_liked_collected / works_count / verify_type / signature / ip_location / first_seen_at / last_seen_at
约束 `(platform, platform_user_id)` 唯一。

**4.2 `content`**（含详情，去重一份）
id / platform / platform_content_id / creator_id↗ / content_type(图文/视频) / title / cover_url / published_at / **topics**(JSON 关联话题) / **platform_tags**(JSON 平台标签) / video_duration / is_ad / ip_location / like_count / comment_count / collect_count / share_count / **body_text**(TEXT 详情正文) / **image_urls**(JSON 全部图片) / detail_fetched_at / **acquisition_hit_count**(获客命中数) / acquisition_checked_at / first_seen_at / last_seen_at(数据更新时间)
约束 `(platform, platform_content_id)` 唯一；索引 creator_id、published_at；标题检索用 FTS5 虚表。
说明：详情并入本表，列表查询只 select 需要的列、不读大字段、免 join。

**4.3 `content_source`**（来源 + 监控时间）
id / content_id↗ / source_type(creator/search/recommend) / source_ref(博主ID或关键词) / first_seen_at(对标=监控时间) / last_seen_at
约束 `(content_id, source_type, source_ref)` 唯一；索引 `(source_type, source_ref, last_seen_at)`。
说明：本地库天然只有本员工数据，无需 owner 字段；对标列表 = 本表(source_type=creator, source_ref=博主) join content。

**4.4 `comment_hit`**（获客命中评论）
id / content_id↗ / comment_text / comment_author / like_count / matched_keyword / published_at / fetched_at
说明：只存命中获客词的评论，命中数回写 content.acquisition_hit_count。

**4.5 `search_suggestion`**（长尾词联想）
id / platform / core_keyword / suggested_keyword / rank / fetched_at；索引 `(platform, core_keyword)`。

**4.6 `collect_task`**（本地采集意图 + 定时）
id / task_type(search/creator_monitor/recommend) / target(关键词或博主ID) / params(JSON) / schedule_seconds(空=一次性) / last_run_at / **last_viewed_at**(算对标新内容) / status / created_at

**4.7 `collect_run`**（执行记录）
id / task_id↗ / status / started_at / finished_at / attempts / item_count / error_summary(JSON)
说明：本地单机执行，无并发争抢，不需要 SKIP LOCKED；本表仅作重试与审计。

---

## 5. 中央库（PostgreSQL）—— 复用现有表 + 改造点

> 全部复用同事重构里已建的表，本阶段只补字段，不新建表。中央**只承载素材库精华**，不存全量流水。

**5.1 `employees` / `users` / `local_agents`**：复用，无改动。

**5.2 `platform_accounts`**（账号池；Profile/会话留本地，不存 Cookie 原文）
复用；**补限速字段**：`daily_quota` / `min_interval_seconds`（反检测底线，E0 用）。

**5.3 `creator_monitors` + `benchmark_groups`**（对标登记 + 定时配置）
复用，作为"对标配置同步"载体——本地对标配置 push 到此，换机/交接可拿回。已含 `check_interval_seconds`/`last_checked_at` 等。

**5.4 `content_identity` + `content_snapshots`**（素材库精华内容，**仅精华非全量**）
复用；**补字段**（建议放 `content_snapshots` 或 `metadata_json`）：关联话题、平台标签、视频时长、是否广告、IP 属地。"加素材库"时 upsert 这一条，按 `(platform, platform_content_id)` 去重，封面用 `stored_cover_path`+`cover_media_status` 缓存。

**5.5 `reference_library_items`**（★素材库收藏，直接复用）
已含：`content_id↗` / `created_by_employee_id`(owner) / `selected_at`(时间) / `material_tags_json`+`manual_tags_json`(标签) / `rating` / `note` / `usage_status` / `status`。
**完全覆盖"谁/何时/什么标签"需求**；主管全局产出 = 按 `created_by_employee_id` 聚合，零改表。同素材多员工收藏 → 内容一份 + 多条 item，标签各存。

**5.6 `manual_tags` / `content_manual_tags` / `keyword_rule*` / `operation_rules`**：复用（标签体系、获客/关键词规则）。

---

## 6. 关系总览

```
中央（只存精华）：
  employees 1─* local_agents / platform_accounts / creator_monitors
  content_identity 1─* content_snapshots
  content_identity 1─* reference_library_items *─1 employees(created_by)
本地（全量流水）：
  creator 1─* content 1─* content_source
                     content 1─* comment_hit
  collect_task 1─* collect_run；search_suggestion（独立小表）
跨库：本地 content 被"加素材库" → 上报中央 upsert content_identity/snapshot + insert reference_library_items
```

---

## 7. 扩展预留蓝图（不建，只留锚点）

- **主管全局产出**（锚点 reference_library_items）：按 `created_by_employee_id` 聚合 join content_identity，零改表。
- **AI**（锚点 content_identity.id）：`content_ai_analysis`(scores, ai_summary, ai_reason, ai_tags) + `content_embedding`(vector, pgvector)。
- **精细化标签**：已有 `manual_tags`/`content_manual_tags`，需分类层级时补 category/parent_id。
- **账号回复**（锚点 platform_accounts.id）：`dm_conversation` + `dm_message` + `comment_reply`。

---

## 8. 设计取舍

1. **本地优先双库**：全量流水留本地（量小、可重采、读取快、省带宽、不周转）；中央只存账号/配置/**筛选打标签的素材库精华**（公司资产、怕丢、要交接、可聚合）。
2. **中央侧全部复用**：素材库直接用 `reference_library_items`，精华内容用 `content_identity`/`content_snapshots`，不再臆造 material/collection——不造轮子。
3. **本地无并发问题**：单机单写，SQLite 足够；不需要 SKIP LOCKED、连接池等中央高并发机制（详见技术方案"数据库改造重估"）。
4. **详情并入本地 content、会话/Profile 留本地、对标更新用字段+查询替代通知表**——减表减 join。
5. **话题/平台标签用 JSON**（采集来）；运营标签用已有 `manual_tags` 体系。
6. **限速字段就地放 platform_accounts**，不建策略表。
7. **可移植**：中央 PG 一步到位（向量/检索都在 PG 内），本地 SQLite 单文件易分发备份；上公网无需换库。
