# 本机 Local Bridge 主地址（Chrome 启动等仍走第一个端口）
# VITE_LOCAL_BRIDGE_URL=http://127.0.0.1:18765

# 登记时扫描的 bridge 端口（逗号分隔）；默认 18765–18774
# 与多个 local_agent 进程的 --bridge-port / 配置 local_bridge.port 对应
# VITE_LOCAL_BRIDGE_PORTS=18765,18766,18767
