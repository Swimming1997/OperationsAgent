# 本地默认：SQLite（无需 Docker / PostgreSQL）
INTEL_ENGINE_DATABASE_URL=sqlite:///./data/intelligence_engine.db
INTEL_ENGINE_AUTH_SECRET_KEY=dev-change-me-in-production
# 本地调试前端时可开启 Header 鉴权（生产请保持 false）
# INTEL_ENGINE_ALLOW_HEADER_AUTH=true
